# Teams Toolkit environment variables — copy to .env.dev and fill in values
# This file is safe to commit. .env.dev (with real values) is gitignored.

# Teams App ID — populated automatically by Teams Toolkit after first provision
TEAMS_APP_ID=

# The publicly reachable URL for the AR Collections REST API
# Local dev: use ngrok or devtunnel to expose http://localhost:3978
#   ngrok:      ngrok http 3978   → copy the https://*.ngrok-free.app URL
#   devtunnel:  devtunnel host -p 3978 --allow-anonymous → copy the URL
# Production: Azure App Service URL, e.g. https://ar-collections.azurewebsites.net
API_BASE_URL=https://your-tunnel-or-host-url-here

# Teams Toolkit environment name (do not change)
TEAMSFX_ENV=dev
